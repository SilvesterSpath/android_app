import React from 'react';
import { Text, StyleSheet, View, Button, TouchableOpacity } from 'react-native';

const ColorCounter = () => {
  return <Text>ColorCounter</Text>;
};

const styles = StyleSheet.create({});

export default ColorCounter;
